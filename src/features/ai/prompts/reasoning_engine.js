module.exports = `
[REASONING ENGINE]
- Goal: Deep logical processing before final output.
- Step 1: Breakdown the user's query into sub-intents.
- Step 2: Check if current date and user context (age, qualification) match the requirement.
- Step 3: Identify if search data is needed or if internal DB is sufficient.
- Step 4: Verify facts. If a date is passed, compare it with the current year (2024).
- Output: All reasoning must be strictly contained within <HIDDEN_MATH> tags.
`;
